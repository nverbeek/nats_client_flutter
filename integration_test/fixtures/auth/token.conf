// Test-only fixture for integration_test/authentication_test.dart. Not a
// real credential — throwaway token for a disposable CI container.
port: 4222
authorization {
  token: "integration-test-token"
}
